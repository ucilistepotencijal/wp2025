@model PetHotel.Models.ServiceType

@{
    ViewData["Title"] = "Nova usluga";
}

<div class="container mt-4">
    <h1 class="text-primary fw-bold">➕ Dodaj novu uslugu</h1>
    <hr />

    <div class="row">
        <div class="col-md-8 col-lg-6">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="card shadow-sm p-4 border-0">
                    <div class="form-group mb-3">
                        <label asp-for="Name" class="control-label fw-bold">Naziv usluge</label>
                        <input asp-for="Name" class="form-control" placeholder="Npr. Noćenje s treningom" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Description" class="control-label fw-bold">Opis</label>
                        <textarea asp-for="Description" class="form-control" rows="3" placeholder="Detaljno opišite što usluga uključuje..."></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Price" class="control-label fw-bold">Cijena po danu (€)</label>
                        <div class="input-group">
                            <input asp-for="Price" class="form-control" type="number" step="0.01" value="0.00" />
                            <span class="input-group-text">€</span>
                        </div>
                        <span asp-validation-for="Price" class="text-danger"></span>
                    </div>

                    <div class="form-check mb-4 p-0 ms-4">
                        <input class="form-check-input" asp-for="IsActive" checked />
                        <label class="form-check-label ms-2 fw-bold">Aktiviraj odmah</label>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success px-4 shadow-sm">Kreiraj uslugu</button>
                        <a asp-action="Index" class="btn btn-outline-secondary">Odustani</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}